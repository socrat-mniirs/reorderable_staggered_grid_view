/// The mode of the reorderable staggered grid view.
enum ReorderableStaggeredGridViewMode {
  /// Lazy building
  normal,

  /// Lazy building with offset animations
  withOffsetAnimations,

  /// Lazy building with preview of reorder operation
  withReorderPreview;
}
